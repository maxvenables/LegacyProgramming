#include <stdio.h> //imports library which includes printf

int main() //Where all our code goes taht needs to be run, required if you need to do anything
{ //Curly braces Encapsulate body of code
    printf("Hello World"); //Outputs Hello World in command prompt. All lines must be ended with a semicolon
    return 0; //return 0 indicates the program has been sucsessfull 
}
