#include <stdio.h>

int main()
{
    int array[10]; //Array length can be defined with number inside []
    
    array[0] = 10; //Assigns value to place in array
    
    printf("First element in array is %d", array[0]);
    
}